import { AsqCurrencyPipe } from './asq-currency.pipe';

describe('AsqCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new AsqCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
