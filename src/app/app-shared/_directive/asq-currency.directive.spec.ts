import { AsqCurrencyDirective } from './asq-currency.directive';

describe('AsqCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new AsqCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
